# Stoqly — Session Handoff (2026-09-01)

Written so a fresh Cursor session can start on **`master_scope_v1`** without re-deriving F2 or the master-runbook state. Continues `docs/handoff-2026-08-26.md`.

**Start here:** checkout `master_scope_v1` at **`8507b77`** (`Merge pull request #23 from karthik-sivam/iOS-F2`). That tip includes all runbook phases 1–7 **and** iOS-F2.

---

## Skills to load first

1. `vishuddhi-beta:org-operating-manual`
2. Task-specific Stoqly skills (`stoqly-*`, not the generic Vishuddhi role):
   - `stoqly-ios-engineer` for code
   - `stoqly-product-owner` / `stoqly-growth` for paywall/listing
   - `stoqly-data-analyst` for Amplitude officialize
   - `stoqly-qa` for device QA
   - `stoqly-android-engineer` only if porting F2

Repo: `~/Documents/My Apps/SmartInventory/smart-inventory`. Scheme `SmartInventory`. App name **Stoqly**. Bundle `com.vishuddhi.stoqly`.

---

## Binding rules (unchanged)

- Never hardcode `isPro = true`. Always `safeSave(context:)`. Scanner = `.fullScreenCover` only.
- Never run `maestro test maestro/run_all.yaml`.
- Live IAP: **₹499/mo, ₹3,999/yr** (StoreKit-localized). Never quote $2.99 / $22.99.
- Manual count + manual sale = Free. SmartSales has **no** monthly cap.
- GitHub Projects not used. Do not invent Coordinators or nested `NavigationStack` in sheet-hosted `ItemDetailView`.
- Do not commit the dirty `AITest/Localizable.xcstrings` or `docs/Stoqly_master_scope_v1_Manual_QA.xlsx` unless the task is explicitly those files — they are leftover local edits, not F2.

---

## What is merged (code)

### Master runbook (already on `master_scope_v1` before this session)

| ID | PR | Status |
|---|---|---|
| Consolidation | — | Merged earlier into `master_scope_v1` |
| T2 in-app onboarding | #15 | Merged. ASC listing side still CEO. |
| D1a / D1a-fix | #16 / #18 | Merged. `barcode_scan_result` includes `code` (not PII). |
| D1c / D1b / D1d | #19-adjacent / #19 / #20 | Merged (CEO merged before F2 session). |
| T5 / T6 | #21 / #22 | Merged. |

Do **not** recut those PRs.

### iOS-F2 — bulk barcode scan — MERGED 2026-09-01

- **PR:** https://github.com/karthik-sivam/smart-inventory/pull/23
- **Merge commit:** `8507b77`
- **Spec:** `docs/cursor-spec-iOS-F2-barcode-bulk.md`
- **CEO console list:** `docs/ceo-manual-work.md`

**Product (Growth option A, locked):**

| | Free | Pro |
|---|---|---|
| Keep-open bulk (queue → review → Save all) | Paywall `source=barcode_bulk` from Storage detail | Storage detail **and** Items toolbar |
| Single scan | Add Item / Edit Item (one-shot). Items toolbar = Scan to Find | Same one-shot inside Add/Edit Item |
| Product lookup (Open Food Facts → UPCItemDB) | **Yes** on single scan | Yes on single scan **and** bulk new rows |
| Ads | After Save (`itemAdded`), never over the camera | Never |

**How to find bulk:** open a **storage** or **Items** (not the Storages **list**). Toolbar barcode with a **plus badge**. Title on camera: “Bulk barcode scan”. Storages-list `+` is still Add Storage.

**Items Pro:** if a storage chip is selected, bulk saves there; if All Storages and more than one storage, a “Save scans to” picker. Match is **that storage only**.

**Pro tradeoff:** Items toolbar is bulk, not Scan to Find. Find a SKU via search or one-shot scan inside Add Item.

**Do not fire** per-beep `barcode_scan_result` from bulk. Session events only:

- `barcode_bulk_scan_started` (`source`: `storage_detail` \| `item_list`)
- `barcode_bulk_scan_completed` (`scanned_count`, `new_count`, `updated_count`, `duration_ms`)
- `barcode_bulk_scan_abandoned` (`stage`, `scanned_count`, `duration_ms`)

---

## Immediate next (fresh session)

### CEO (no code)

Numbered steps live in `docs/ceo-manual-work.md`:

1. **T1 / T2 listing / T3 / T4** — App Store Connect listing honesty (unlimited items, cloud sync Pro, AI reorder, “no manual entry”).
2. **Amplitude project 832993** — officialize the three `barcode_bulk_scan_*` events (create unexpected → update official). This session’s Amplitude MCP was unauthenticated.
3. **T2 native review** — 14 language drafts in `Localizable.xcstrings` still `needs_review`.
4. **Manual QA** — `docs/Stoqly_master_scope_v1_Manual_QA.xlsx` against a `master_scope_v1` device build (⌘R, not an old TestFlight).
5. **F1** — confirm 7-day trial on a sandbox device if not already done.

### Cursor / engineering (only if CEO asks)

| Item | Notes |
|---|---|
| **Android F2 port** | `stoqly-android-engineer`. CameraX + ML Kit full-screen; never a bottom sheet. Same Free/Pro split. |
| **F2b** | Name chips on live camera; still-photo multi-barcode. **Not** this release. |
| **Phase 7 Live AI Camera** | Shelf **product** identity, not barcodes. Separate from F2. |
| **Scan-to-Find for Pro** | Removed from Items toolbar when bulk took that icon. Add a labeled action only if CEO wants it back. |
| **Durable scan history** | Do not put “history” on the paywall until this screen exists. |
| **Maestro flow-82 collision** | Hygiene in `run_all.yaml`; non-blocking. Do not run `run_all`. |

There is **no** in-flight D1 Cursor pause. The 2026-08-26 handoff’s “reply continue for D1c” is stale.

---

## Local dirt a fresh session must ignore

Uncommitted on disk (do not mix into a random PR):

- `AITest/Localizable.xcstrings` (dirty before F2)
- `docs/Stoqly_master_scope_v1_Manual_QA.xlsx`
- Untracked: `docs/cursor-master-runbook.md`, `docs/cursor-spec-iOS-D1-*.md`, `docs/cursor-spec-iOS-T2-onboarding-copy.md`, `docs/handoff-2026-08-26.md`, `.cursor/mcp.json`

The D1/T2 specs may already be on `master_scope_v1` from earlier merges; the untracked copies are local duplicates/drafts. Diff before adding.

---

## How to continue in a new chat

Paste this:

> Start from `docs/handoff-2026-09-01.md`. Repo is on `master_scope_v1` at `8507b77` (F2 merged, PR #23). Do not recut D1/T5/T6/F2. Next engineering work only if I name it. CEO console work is `docs/ceo-manual-work.md`. Load stoqly-ios-engineer + org-operating-manual.

---

## Beyond this session

- Retention gates still OPEN (2026-08-21 audit). Paid growth still blocked.
- `identify_product` / `sheet_import` AI events still unwired (no LLM / local CSV) — see `docs/event-taxonomy.md`.
- Splash + Auth dark-mode audit still open.
- `PostLoginOnboardingView` (empty storages) still pending.
