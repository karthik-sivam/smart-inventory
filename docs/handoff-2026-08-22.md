# Stoqly — Session Handoff (2026-08-22)

Written by the previous Claude Cowork session so a fresh session can pick up the master_scope_v1 program of work without re-deriving state.

## The program of work
Stoqly is running the "master_scope_v1" push — a bundle of Wave 0 activation fixes (Wave 1 stories iOS-A1..A5) plus a QA-driven fix batch (Wave 1.5: F1, F2, F3, F4, F7, F8). Everything lands on the `master_scope_v1` git branch via per-story feature branches + a consolidation merge.

## Skills the fresh session must load first
1. `vishuddhi-beta:org-operating-manual`
2. `vishuddhi-beta:scrum-master` (generic Scrum stays generic per CEO decision)
3. Task-specific Stoqly skills — always the `stoqly-*` variant, never the generic `vishuddhi-beta:*` role skill:
   - `stoqly-product-owner` — product decisions, PRDs
   - `stoqly-ios-engineer` — iOS code, Cursor specs, release-prep
   - `stoqly-android-engineer` — Android parity (deferred until activation gate moves)
   - `stoqly-backend-engineer` — Firestore, Cloud Functions, LLM proxy
   - `stoqly-qa` — test plans, Maestro (currently DEFERRED per cost decision)
   - `stoqly-product-designer` — UX, screen specs, dark-mode
   - `stoqly-data-analyst` — Amplitude 832993, funnel reads, gate ledger
   - `stoqly-growth` — ASO, listing copy, Meta targeting, positioning (paid ads BLOCKED)

## Key facts the CEO has corrected on the record (skills need re-syncing)
- Live IAP pricing: **₹499/mo, ₹3,999/yr** (StoreKit-localized per storefront). Do NOT quote $2.99/$22.99 — that's stale.
- There is **NO free trial YET**. iOS-F1 introduces a real 7-day free trial via Firestore `manualProUntil` + StoreKit's existing FreeTrial introductoryOffer. Update all skill copy to say "7-day free trial (in progress — landing via iOS-F1)."
- SmartSales has **no monthly cap** — this is intentional. Do NOT propose adding one.
- Currency: all app-facing price strings must come from StoreKit's `localizedPriceString` — never hardcoded.
- Manual counts + manual sales are FREE (always). Manual PURCHASE entry is currently a paywall dead-end; iOS-C1 fixes this once decided (recommended: add free manual Purchase entry mirroring Sales).
- Maestro `run_all` is DEFERRED (credits). Only run individually named single flows if strictly required.
- GitHub Projects NOT used. Google Sheet is the tracker.

## Retention gate ledger (audit, 2026-08-21) — nothing has moved yet
- new→storage→item activation: 10.8% (target ≥25%)
- add-item completion: 60% (target ≥80%)
- Smart Count completion: 29.6% (target ≥55%)
- Week-1 retention: 13.3% (target ≥20% × 2 cohorts)
- Weekly-loop shops: 0 (target ≥5 × 4 weeks)

Growth paid spend stays BLOCKED until all gates hold.

## Wave 1 status
All six branches complete, pushed to origin, NOT merged into master_scope_v1:
- iOS-A1 — revert isPro hardcode (964b77c)
- iOS-A2 — remove barcode debug prints (0ce864f)
- iOS-A3 — Splash + Auth dark-mode fix (7f71e24)
- iOS-A4 — add-item Save works with defaults (631f2b3) — **A4 UI incomplete; F7/A4b addresses**
- iOS-A5-pre — add confidence to SmartSales AI response (d646866)
- iOS-A5 — SmartCount + SmartSales review UX repair (9ce7f78)

## Wave 1.5 status
- **iOS-F1** trial: branch cut, NOT started. Cursor prompt in prior session; re-paste from handoff §"Cursor prompts to re-run".
- **iOS-F2** barcode bulk scan: needs Product Owner PRD before Cursor spec.
- **iOS-F3** photo paywall auto-dismiss: **CHECK REPO STATE FIRST.** Was sitting uncommitted on iOS-A1; CEO instructed to move to `iOS-F3` branch. If iOS-F3 branch exists on origin, it's done. Otherwise rescue per handoff §"Cleanup log".
- **iOS-F4** AdMob instrumentation + diagnosis: DONE (9b8d6f6), pushed. Awaiting merge. Diagnostic report at `smart-inventory/docs/adman-diagnostic-2026-08-22.md`.
- **iOS-F7 / iOS-A4b** progressive-disclosure UI: `iOS-A4b` branch cut, NOT started. Re-paste Cursor prompt.
- **iOS-F8** SmartCount/SmartSales entry chips: DONE (6b263d0), pushed. Awaiting merge.

## Wave 1 QA — A1 findings summary
CEO ran A1.1–A1.14 partially. Real bugs surfaced (now Wave 1.5):
- A1.3 → F3 (photo paywall auto-dismiss).
- A1.5 → F5 CLOSED (no cap, intentional).
- A1.7 → F2 (barcode bulk not built but promised — decision: build).
- A1.8 → confirmed pass (thin Pro trigger; noted for F6 packaging review).
- A1.13 → F1 (real trial to be built).
- A1.14 → F4 (ads not visible + no telemetry).

## Immediate next actions (in order)
1. **Verify iOS-F3 rescue** — check origin has `iOS-F3` branch with the ItemPhotoSection fix committed.
2. **Kick off iOS-F1 and iOS-A4b Cursor sessions** — worktrees may already exist; specs are in the prior session transcript.
3. **CEO to run manual QA** on A2, A3, A4 (with A4b once shipped), A5-pre, A5 per checklist in prior session's "manual QA list for wave 1" turn.
4. **F2 barcode bulk PRD** — needs Product Owner writeup before Cursor spec.
5. **Consolidate all ready branches into master_scope_v1** — consolidation prompt in prior session, order: F3, F4, F1 (once done), A2, A1, A3, A4, A4b, A5-pre, A5, F7, F8.

## Trust-debt items still open (P0 for master_scope_v1 release)
- iOS-A1 audit: 30 files changed incl. 24 Maestro flow edits — need eyeballing to confirm no regression hidden.
- Barcode bulk (F2) promised in paywall but not built — must land before shipping.
- Trial contradiction (F1) — StoreKit config declares FreeTrial but code + LLM prompts explicitly say "no trial" — must land before shipping.

## Cursor prompts to re-run
The prior session drafted worktree-aware Cursor prompts for iOS-F1, iOS-F3, iOS-F4, iOS-F7 (as iOS-A4b), iOS-F8, and the consolidation. These prompts follow the pattern:
- Set up worktree branched off `master_scope_v1`
- Load `stoqly-*` skills
- Precise scope + files + acceptance
- Verify → append to `automation_results.rtf` → commit → push → open PR

If the fresh session doesn't have the transcript, ask the CEO to paste back the F1 and A4b prompts; otherwise re-derive from this doc's summary and the SKILL.md files at `smart-inventory/skills/`.

## Cleanup log (from prior session)
- Six stale worktree bookkeeping dirs were quarantined to `.git/worktrees/_stale_to_delete/`. CEO to `rm -rf` that folder from Mac terminal when convenient (device shell can't delete).
- One leftover packaging tarball at `smart-inventory/skills/_to_delete/`. Same treatment.

## Tracker
Google Sheet with per-story rows (ID, Sprint, Priority, Track, Story, Type, Role, Estimate, Depends on, Files touched, Status, Acceptance criteria, Cursor session URL, Result, Notes). CEO owns the sheet.
